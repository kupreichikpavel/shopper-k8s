apiVersion: v1
kind: Service
metadata:
  name: order-db
  namespace: shopper
  labels:
    app: order-db
spec:
  type: ClusterIP
  selector:
    app: order-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432