apiVersion: v1
kind: ConfigMap
metadata:
  name: user-service-config
  namespace: shopper
data:
  SPRING_PROFILES_ACTIVE: docker
  POSTGRES_HOST: postgres
  POSTGRES_PORT: "5432"
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  KEYCLOAK_ISSUER_URI: http://keycloak:8080/realms/shopper
